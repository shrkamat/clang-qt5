--- a/mkspecs/linux-clang-libc++/qmake.conf
+++ b/mkspecs/linux-clang-libc++/qmake.conf
@@ -5,6 +5,6 @@
 include(../linux-clang/qmake.conf)

 QMAKE_CXXFLAGS    += -stdlib=libc++
-QMAKE_LFLAGS      += -stdlib=libc++
+QMAKE_LFLAGS      += -stdlib=libc++ -lc++abi

 load(qt_config)
